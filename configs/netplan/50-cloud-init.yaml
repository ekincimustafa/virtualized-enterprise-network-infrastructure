# ==============================================================================
# RECONSTRUCTED NETWORK CONFIGURATION TEMPLATE
#
# Verified final compute-node state:
#   Ubuntu Server 26.04 LTS
#   Interface: ens160
#   IPv4:      10.10.20.50/24
#   Gateway:   10.10.20.1
#
# The resolver addresses below were documented earlier in the project and were
# not independently re-validated during the final evidence pass.
# Target path: /etc/netplan/50-cloud-init.yaml
# ==============================================================================

network:
  version: 2
  ethernets:
    ens160:
      dhcp4: no
      addresses:
        - 10.10.20.50/24
      routes:
        - to: default
          via: 10.10.20.1
      nameservers:
        addresses:
          - 10.10.20.1
          - 8.8.8.8
