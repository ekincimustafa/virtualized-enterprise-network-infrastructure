# ==============================================================================
# RECONSTRUCTED CONFIGURATION TEMPLATE BASED ON VERIFIED LIVE SETTINGS
# Target Path: /etc/systemd/resolved.conf on Compute Node (Ubuntu Server 24.04)
# ==============================================================================

[Resolve]
# Disables systemd's local DNS stub listener on 127.0.0.53:53,
# allowing the Pi-hole container to bind directly to host port 53.
DNSStubListener=no