# ==============================================================================
# RECONSTRUCTED CONFIGURATION TEMPLATE BASED ON A VERIFIED LIVE SETTING
# Target path: /etc/systemd/resolved.conf
# Compute node: Ubuntu Server 26.04 LTS
# ==============================================================================

[Resolve]
# Disable systemd-resolved's local DNS stub listener on 127.0.0.53:53 so the
# Pi-hole container can publish host port 53/TCP and 53/UDP.
DNSStubListener=no
